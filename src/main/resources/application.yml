spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/checker
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

telegram:
  bot:
    username: Arizona Helper
    token: 8093397224:AAHd2F77_4RRImaEXnkmF2UrCgNtUcmUmWQ

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework: INFO
    org.hibernate: INFO
